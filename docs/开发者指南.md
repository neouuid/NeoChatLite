# NeoChat 开发者指南

> 最后更新: 2026-04-13

## 目录

- [概述](#概述)
- [项目架构](#项目架构)
- [开发环境搭建](#开发环境搭建)
- [后端开发](#后端开发)
- [前端开发](#前端开发)
- [代码规范](#代码规范)
- [测试指南](#测试指南)
- [Git 工作流](#git-工作流)
- [常见问题](#常见问题)

---

## 概述

本文档为 NeoChat 项目的开发者提供完整的开发指南，包括项目架构、开发流程、代码规范等内容。

### 项目定位

NeoChat 是一款现代化的跨平台即时通讯应用，支持：
- iOS / Android 移动端
- Windows / macOS 桌面端
- WebSocket 实时通信
- 单聊 / 群聊 / 音视频通话

---

## 项目架构

### 整体架构

```
neochat/
├── backend/              # 后端 (Go)
│   ├── cmd/             # 应用入口
│   │   └── api-gateway/ # API 网关
│   ├── internal/        # 内部服务
│   │   ├── auth/       # 认证模块
│   │   ├── user/       # 用户/好友模块
│   │   └── chat/       # 聊天/群组模块
│   ├── pkg/            # 公共包
│   │   ├── config/     # 配置
│   │   ├── database/   # 数据库
│   │   ├── redis/      # Redis
│   │   ├── response/   # API 响应
│   │   ├── logger/     # 日志
│   │   └── utils/      # 工具函数
│   └── configs/        # 配置文件
├── client/              # 客户端 (React Native)
│   ├── shared/         # 共享代码 (85-90%)
│   │   ├── components/ # 共享组件
│   │   ├── stores/     # Zustand 状态管理
│   │   ├── services/   # API 服务
│   │   ├── hooks/      # React Hooks
│   │   ├── utils/      # 工具函数
│   │   ├── constants/  # 常量
│   │   └── types/      # TypeScript 类型
│   ├── mobile/         # 移动端
│   │   ├── src/
│   │   │   ├── screens/     # 页面
│   │   │   └── navigation/  # 导航
│   │   └── App.tsx
│   └── desktop/        # 桌面端
│       ├── src/
│       │   ├── screens/     # 窗口
│       │   └── components/  # 桌面组件
│       └── App.tsx
├── docs/                # 文档
├── design/              # 设计文件 (Pencil)
└── README.md
```

### 技术栈

#### 后端
- **语言**: Go 1.21+
- **Web 框架**: Gin
- **ORM**: GORM
- **数据库**: PostgreSQL
- **缓存**: Redis
- **实时通信**: WebSocket (gorilla/websocket)
- **认证**: JWT
- **文档**: Swagger
- **测试**: testing + testify

#### 前端
- **框架**: React Native 0.72+
- **语言**: TypeScript 5+
- **状态管理**: Zustand
- **导航**: React Navigation
- **UI 组件**: React Native Elements
- **测试**: Jest + Testing Library
- **代码规范**: ESLint + Prettier

---

## 开发环境搭建

详细的开发环境搭建步骤请参考 [开发环境搭建.md](开发环境搭建.md)。

### 快速开始

```bash
# 1. 克隆代码
git clone https://github.com/neochat/neochat.git
cd neochat

# 2. 启动数据库 (Docker)
docker-compose up -d postgres redis

# 3. 后端开发
cd backend
cp configs/config.yaml.example config.yaml
go run ./cmd/api-gateway

# 4. 前端开发 (移动端)
cd ../client/mobile
npm install
npm start

# 5. 前端开发 (桌面端)
cd ../client/desktop
npm install
npm run windows  # 或 npm run macos
```

---

## 后端开发

### 项目结构说明

#### 目录结构

```
backend/
├── cmd/
│   └── api-gateway/
│       ├── main.go      # 主入口
│       └── routes.go    # 路由设置
├── internal/
│   ├── auth/
│   │   ├── model.go     # 数据模型
│   │   ├── repository.go # 数据访问层
│   │   ├── service.go   # 业务逻辑层
│   │   ├── handler.go   # HTTP 处理器
│   │   └── middleware.go # 中间件
│   ├── user/            # 用户/好友模块 (同 auth 结构)
│   └── chat/            # 聊天/群组模块 (同 auth 结构)
└── pkg/
    ├── config/          # 配置
    ├── database/        # 数据库
    ├── redis/           # Redis
    ├── response/        # API 响应
    ├── logger/          # 日志
    ├── utils/           # 工具函数
    └── test/            # 测试工具
```

### 开发规范

#### 1. 分层架构

采用标准的三层架构：

```
Handler (HTTP 层)
    ↓
Service (业务逻辑层)
    ↓
Repository (数据访问层)
    ↓
Database
```

#### 2. 代码组织

每个模块包含以下文件：

- `model.go` - 数据模型定义
- `repository.go` - 数据库操作
- `service.go` - 业务逻辑
- `handler.go` - HTTP 处理器
- `middleware.go` - 中间件（如需要）

#### 3. 模型定义

```go
package auth

import (
	"time"
	"github.com/google/uuid"
	"gorm.io/gorm"
)

type VerificationToken struct {
	ID        uuid.UUID      `gorm:"type:uuid;primary_key" json:"id"`
	UserID    uuid.UUID      `gorm:"type:uuid;index;not null" json:"user_id"`
	Token     string         `gorm:"type:varchar(255);uniqueIndex;not null" json:"-"`
	Type      string         `gorm:"type:varchar(50);index;not null" json:"type"`
	ExpiresAt time.Time      `gorm:"index;not null" json:"expires_at"`
	CreatedAt time.Time      `gorm:"index" json:"created_at"`
}

func (v *VerificationToken) BeforeCreate(tx *gorm.DB) error {
	if v.ID == uuid.Nil {
		v.ID = uuid.New()
	}
	return nil
}

func (v *VerificationToken) IsExpired() bool {
	return time.Now().After(v.ExpiresAt)
}
```

#### 4. Repository 层

```go
package auth

import (
	"errors"
	"github.com/google/uuid"
	"gorm.io/gorm"
)

type Repository struct {
	db *gorm.DB
}

func NewRepository(db *gorm.DB) *Repository {
	return &Repository{db: db}
}

func (r *Repository) CreateVerificationToken(token *VerificationToken) error {
	return r.db.Create(token).Error
}

func (r *Repository) GetVerificationToken(token string, tokenType string) (*VerificationToken, error) {
	var vt VerificationToken
	err := r.db.Where("token = ? AND type = ?", token, tokenType).
		First(&vt).Error
	if err != nil {
		return nil, err
	}
	return &vt, nil
}

func (r *Repository) DeleteVerificationToken(id uuid.UUID) error {
	return r.db.Delete(&VerificationToken{ID: id}).Error
}
```

#### 5. Service 层

```go
package auth

import (
	"errors"
	"github.com/google/uuid"
	"github.com/neochat/backend/pkg/config"
	"gorm.io/gorm"
)

type Service struct {
	repo   *Repository
	config *config.Config
}

func NewService(repo *Repository, cfg *config.Config) *Service {
	return &Service{repo: repo, config: cfg}
}

func (s *Service) VerifyEmail(req *VerifyEmailRequest) error {
	vt, err := s.repo.GetVerificationToken(req.Code, TokenTypeEmailVerification)
	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return errors.New("invalid or expired code")
		}
		return err
	}

	if vt.IsExpired() {
		return errors.New("code has expired")
	}

	return s.repo.DeleteVerificationToken(vt.ID)
}
```

#### 6. Handler 层

```go
package auth

import (
	"net/http"
	"github.com/gin-gonic/gin"
	"github.com/neochat/backend/pkg/response"
)

type Handler struct {
	service *Service
}

func NewHandler(service *Service) *Handler {
	return &Handler{service: service}
}

func (h *Handler) VerifyEmail(c *gin.Context) {
	var req VerifyEmailRequest
	if err := c.ShouldBindJSON(&req); err != nil {
		response.Error(c, http.StatusBadRequest, "invalid_request", "请求参数错误")
		return
	}

	if err := h.service.VerifyEmail(&req); err != nil {
		response.Error(c, http.StatusBadRequest, "verify_failed", err.Error())
		return
	}

	response.Success(c, nil, "邮箱验证成功")
}
```

### API 响应规范

使用统一的响应格式：

```go
// 成功响应
response.Success(c, data, message)

// 错误响应
response.Error(c, statusCode, errorCode, message)
```

响应格式：

```json
{
  "success": true,
  "data": { ... },
  "message": "操作成功"
}
```

```json
{
  "success": false,
  "error": "invalid_request",
  "message": "请求参数错误"
}
```

### 添加新模块

#### 步骤

1. **创建模块目录**
   ```bash
   mkdir internal/mymodule
   ```

2. **创建模型** (`model.go`)
   - 定义数据结构
   - 添加 GORM 标签
   - 添加 `BeforeCreate` 钩子（如需要）

3. **创建 Repository** (`repository.go`)
   - 定义 Repository 结构体
   - 实现 `NewRepository` 函数
   - 添加 CRUD 方法

4. **创建 Service** (`service.go`)
   - 定义 Service 结构体
   - 实现 `NewService` 函数
   - 添加业务逻辑方法

5. **创建 Handler** (`handler.go`)
   - 定义 Handler 结构体
   - 实现 `NewHandler` 函数
   - 添加 HTTP 处理器

6. **在 main.go 中注册路由**
   - 初始化各层
   - 设置路由

### 数据库迁移

添加新模型后，GORM 会自动迁移：

```go
// pkg/database/database.go
func AutoMigrate() error {
	return DB.AutoMigrate(
		&user.User{},
		&auth.VerificationToken{},
		&chat.MyNewModel{}, // 添加新模型
	)
}
```

---

## 前端开发

### 项目结构说明

#### 共享代码 (shared)

```
client/shared/
├── components/          # 共享组件
│   ├── Avatar.tsx
│   ├── Button.tsx
│   ├── Input.tsx
│   ├── MessageList.tsx
│   └── ConversationItem.tsx
├── stores/             # Zustand 状态管理
│   ├── useAuthStore.ts
│   ├── useChatStore.ts
│   └── useUserStore.ts
├── services/           # API 服务
│   ├── api.ts         # API 基础配置
│   ├── authService.ts
│   ├── chatService.ts
│   └── userService.ts
├── hooks/              # React Hooks
│   ├── useAuth.ts
│   ├── useChat.ts
│   ├── useMediaPicker.ts
│   └── useMediaUpload.ts
├── utils/              # 工具函数
│   ├── format.ts
│   ├── validation.ts
│   └── storage.ts
├── constants/          # 常量
│   ├── colors.ts
│   ├── spacing.ts
│   └── typography.ts
└── types/              # TypeScript 类型
    ├── index.ts
    ├── auth.ts
    ├── chat.ts
    └── user.ts
```

### 开发规范

#### 1. 组件开发

```tsx
// client/shared/components/Avatar.tsx
import React, { useMemo } from 'react';
import { View, Text, StyleSheet, Image } from 'react-native';
import { COLORS, SPACING } from '../constants';

interface AvatarProps {
  uri?: string;
  nickname: string;
  size?: 'sm' | 'md' | 'lg';
  style?: any;
}

const sizeMap = {
  sm: 32,
  md: 48,
  lg: 64,
};

export const Avatar: React.FC<AvatarProps> = ({
  uri,
  nickname,
  size = 'md',
  style,
}) => {
  const sizeValue = sizeMap[size];

  const initials = useMemo(() => {
    return nickname?.charAt(0).toUpperCase() || '?';
  }, [nickname]);

  if (uri) {
    return (
      <Image
        source={{ uri }}
        style={[styles.avatar, { width: sizeValue, height: sizeValue, borderRadius: sizeValue / 2 }, style]}
      />
    );
  }

  return (
    <View
      style={[
        styles.avatar,
        styles.placeholder,
        { width: sizeValue, height: sizeValue, borderRadius: sizeValue / 2 },
        style,
      ]}
    >
      <Text style={[styles.initials, { fontSize: sizeValue * 0.4 }]}>
        {initials}
      </Text>
    </View>
  );
};

const styles = StyleSheet.create({
  avatar: {
    backgroundColor: COLORS.primary,
  },
  placeholder: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  initials: {
    color: '#fff',
    fontWeight: '600',
  },
});
```

#### 2. Store 开发 (Zustand)

```typescript
// client/shared/stores/useAuthStore.ts
import { create } from 'zustand';
import { User } from '../types';
import { authService } from '../services';

interface AuthState {
  user: User | null;
  token: string | null;
  isLoading: boolean;
  isAuthenticated: boolean;

  // Actions
  setUser: (user: User | null) => void;
  setToken: (token: string | null) => void;
  setLoading: (loading: boolean) => void;
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
}

export const useAuthStore = create<AuthState>((set, get) => ({
  user: null,
  token: null,
  isLoading: false,
  isAuthenticated: false,

  setUser: (user) => set({ user, isAuthenticated: !!user }),
  setToken: (token) => set({ token }),
  setLoading: (isLoading) => set({ isLoading }),

  login: async (email, password) => {
    set({ isLoading: true });
    try {
      const response = await authService.login({ email, password });
      if (response.success && response.data) {
        const { token, user } = response.data;
        set({ user, token, isAuthenticated: true });
      }
    } finally {
      set({ isLoading: false });
    }
  },

  logout: () => {
    set({ user: null, token: null, isAuthenticated: false });
  },
}));
```

#### 3. Service 开发

```typescript
// client/shared/services/authService.ts
import { api } from './api';
import type {
  LoginRequest,
  LoginResponse,
  RegisterRequest,
  RegisterResponse,
  AuthResponse,
} from '../types';

export const authService = {
  async login(data: LoginRequest): Promise<AuthResponse<LoginResponse>> {
    const response = await api.post('/auth/login', data);
    return response.data;
  },

  async register(data: RegisterRequest): Promise<AuthResponse<RegisterResponse>> {
    const response = await api.post('/auth/register', data);
    return response.data;
  },

  async getProfile(): Promise<AuthResponse<any>> {
    const response = await api.get('/auth/profile');
    return response.data;
  },
};
```

#### 4. API 基础配置

```typescript
// client/shared/services/api.ts
import axios from 'axios';
import { useAuthStore } from '../stores';

const API_BASE_URL = __DEV__
  ? 'http://localhost:8080/api/v1'
  : 'https://api.your-domain.com/api/v1';

export const api = axios.create({
  baseURL: API_BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 请求拦截器 - 添加认证 token
api.interceptors.request.use(
  (config) => {
    const token = useAuthStore.getState().token;
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器 - 处理认证错误
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      useAuthStore.getState().logout();
    }
    return Promise.reject(error);
  }
);
```

### 添加新页面

#### 移动端

1. **创建页面组件**
   ```tsx
   // client/mobile/src/screens/MyNewScreen.tsx
   import React from 'react';
   import { View, Text, StyleSheet } from 'react-native';
   import { SafeAreaView } from 'react-native-safe-area-context';
   import { COLORS, TYPOGRAPHY } from '@neochat/shared';

   export const MyNewScreen: React.FC = () => {
     return (
       <SafeAreaView style={styles.container}>
         <Text style={styles.title}>我的新页面</Text>
       </SafeAreaView>
     );
   };

   const styles = StyleSheet.create({
     container: {
       flex: 1,
       backgroundColor: COLORS.dark.background,
     },
     title: {
       fontSize: TYPOGRAPHY.sizes.lg,
       color: COLORS.dark.text.primary,
     },
   });
   ```

2. **在导航中注册**
   ```tsx
   // client/mobile/src/navigation/index.tsx
   <Stack.Screen
     name="MyNew"
     component={MyNewScreen}
     options={{ title: '新页面' }}
   />
   ```

#### 桌面端

1. **创建窗口组件**
   ```tsx
   // client/desktop/src/screens/MyNewWindow.tsx
   import React from 'react';
   import { View, Text, StyleSheet } from 'react-native';
   import { COLORS, TYPOGRAPHY } from '@neochat/shared';

   export const MyNewWindow: React.FC = () => {
     return (
       <View style={styles.container}>
         <Text style={styles.title}>我的新窗口</Text>
       </View>
     );
   };

   const styles = StyleSheet.create({
     container: {
       flex: 1,
       backgroundColor: COLORS.dark.background,
     },
     title: {
       fontSize: TYPOGRAPHY.sizes.lg,
       color: COLORS.dark.text.primary,
     },
   });
   ```

---

## 代码规范

### Go 代码规范

#### 命名规范

- **包名**: 小写单词，不使用下划线或驼峰
  ```go
  package auth      // ✅
  package Auth      // ❌
  package auth_svc  // ❌
  ```

- **函数名**: 导出使用驼峰，私有使用小写
  ```go
  func NewService()   // ✅ 导出
  func doSomething()  // ✅ 私有
  ```

- **常量**: 驼峰或大写带下划线
  ```go
  const TokenTypeEmailVerification = "email_verification"  // ✅
  const MAX_SIZE = 100  // ✅
  ```

#### 格式化

使用 `gofmt` 自动格式化：

```bash
gofmt -w .
```

#### 注释

- 导出函数必须添加注释
- 注释以名称开头，说明功能
  ```go
  // NewService 创建认证服务实例
  func NewService(repo *Repository, cfg *config.Config) *Service {
  ```

### TypeScript 代码规范

#### 命名规范

- **组件**: PascalCase
  ```tsx
  export const MyComponent: React.FC = () => {}  // ✅
  ```

- **函数/变量**: camelCase
  ```typescript
  const myVariable = 123;           // ✅
  function doSomething() {}           // ✅
  ```

- **常量**: UPPER_SNAKE_CASE 或 PascalCase
  ```typescript
  export const MAX_COUNT = 100;       // ✅
  export const COLORS = { ... };       // ✅
  ```

- **类型/接口**: PascalCase，前缀 `I` 可选
  ```typescript
  interface User { ... }               // ✅
  type LoginRequest = { ... };         // ✅
  ```

#### 格式化

使用 Prettier 自动格式化：

```bash
npm run format
```

#### ESLint

运行 ESLint 检查：

```bash
npm run lint
npm run lint:fix  # 自动修复
```

---

## 测试指南

详细的测试指南请参考 [测试指南.md](测试指南.md)。

### 后端测试

```bash
cd backend

# 运行所有测试
go test ./...

# 运行特定包
go test ./pkg/utils -v

# 运行集成测试
RUN_INTEGRATION_TESTS=true go test ./internal/auth -v

# 查看覆盖率
go test ./... -coverprofile=coverage.out
go tool cover -html=coverage.out
```

### 前端测试

```bash
cd client/shared

# 运行所有测试
npm test

# 监听模式
npm run test:watch

# 查看覆盖率
npm run test:coverage
```

---

## Git 工作流

### 分支策略

- `main` - 主分支，稳定版本
- `develop` - 开发分支
- `feature/*` - 功能分支
- `bugfix/*` - 修复分支
- `hotfix/*` - 紧急修复

### 提交规范

使用约定式提交格式：

```
<type>(<scope>): <subject>

<body>

<footer>
```

Type 类型：
- `feat` - 新功能
- `fix` - 修复 bug
- `docs` - 文档更新
- `style` - 代码格式调整
- `refactor` - 重构
- `test` - 测试相关
- `chore` - 构建/工具相关

示例：

```bash
git commit -m "feat(auth): 添加邮箱验证功能

- 添加 VerificationToken 模型
- 添加邮箱验证 API 端点
- 添加邮件发送服务"
```

### 开发流程

1. **创建功能分支**
   ```bash
   git checkout -b feature/my-new-feature
   ```

2. **开发并提交**
   ```bash
   git add .
   git commit -m "feat: 添加新功能"
   ```

3. **推送到远程**
   ```bash
   git push -u origin feature/my-new-feature
   ```

4. **创建 Pull Request**

5. **代码审查和合并**

---

## 常见问题

### 后端

**Q: 如何添加新的 API 端点？**

A:
1. 在 handler.go 中添加处理函数
2. 在 main.go 的路由中注册
3. 在 service.go 中添加业务逻辑
4. 在 repository.go 中添加数据访问（如需要）

**Q: 数据库迁移失败怎么办？**

A:
1. 检查模型定义是否正确
2. 检查数据库连接
3. 删除数据库重新创建（开发环境）
4. 手动编写迁移 SQL

**Q: 如何调试 WebSocket？**

A:
1. 使用浏览器的开发者工具查看 WebSocket 连接
2. 使用 `wscat` 命令行工具测试
3. 添加日志调试消息

### 前端

**Q: Metro bundler 缓存问题？**

A:
```bash
cd client/mobile
npm start -- --reset-cache
```

**Q: 依赖安装失败？**

A:
```bash
# 清除缓存
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

**Q: TypeScript 类型错误？**

A:
1. 检查类型定义是否正确
2. 运行 `npx tsc --noEmit` 查看详细错误
3. 检查 `shared` 包是否正确链接

---

## 更多资源

- [README](../README.md) - 项目概述
- [架构设计](架构设计.md) - 架构详情
- [数据库设计](数据库设计.md) - 数据库设计
- [API设计](API设计.md) - API 设计
- [测试指南](测试指南.md) - 测试指南
- [开发环境搭建](开发环境搭建.md) - 环境搭建
- [API使用示例](API使用示例.md) - API 示例
- [部署文档](部署文档.md) - 部署指南
